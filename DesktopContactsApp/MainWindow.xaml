<Window x:Class="DesktopContactsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopContactsApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="500">
    <StackPanel Margin="10">
        <Button Content="New Contact" Click="Button_Click"/>
        <TextBox TextChanged="TextBox_TextChanged" Margin="10"/>

        <ListView x:Name="contactsListView" Margin="10" Height="250">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock FontSize="25" Text="{Binding Name}" FontWeight="Bold" />
                        <TextBlock FontSize="15" Text="{Binding Phone}" />
                        <TextBlock FontSize="10" Text="{Binding Email}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>
